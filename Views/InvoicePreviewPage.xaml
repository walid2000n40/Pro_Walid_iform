<Page
    x:Class="ProWalid.Views.InvoicePreviewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource AppFontFamily}"
    FlowDirection="RightToLeft">

    <Page.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#F6FAFF" Offset="0"/>
            <GradientStop Color="#EAF3FF" Offset="0.55"/>
            <GradientStop Color="#DDEBFF" Offset="1"/>
        </LinearGradientBrush>
    </Page.Background>

    <Grid Padding="10,8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="#FFFFFF"
                BorderBrush="#D9E6F4"
                BorderThickness="1"
                CornerRadius="16"
                Padding="10,6"
                Margin="0,0,0,6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                        Command="{Binding GoBackCommand}"
                        Background="#EFF6FF"
                        Foreground="#1D4ED8"
                        BorderBrush="#BFDBFE"
                        BorderThickness="1"
                        CornerRadius="8"
                        MinHeight="28"
                        Padding="10,3">
                    <TextBlock Text="رجوع" FontSize="12"/>
                </Button>

                <StackPanel Grid.Column="2" HorizontalAlignment="Right" Spacing="1">
                    <TextBlock Text="معاينة الفاتورة"
                               FontSize="18"
                               FontWeight="Bold"
                               Foreground="#123B66"
                               TextAlignment="Right"/>
                    <TextBlock Text="عرض مباشر لمعاينة الفاتورة مع أوامر الحفظ والطباعة فقط"
                               FontSize="10"
                               Foreground="#52708E"
                               Visibility="Collapsed"
                               TextAlignment="Right"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1"
              RowSpacing="6"
              FlowDirection="LeftToRight">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Background="#FFFFFF"
                    BorderBrush="#D9E6F4"
                    BorderThickness="1"
                    CornerRadius="16"
                    Padding="10,6">
                <Grid ColumnSpacing="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                        <Button Click="PrintInvoiceButton_Click"
                                Background="#E8F5E9"
                                Foreground="#1B5E20"
                                BorderBrush="#A5D6A7"
                                BorderThickness="1"
                                CornerRadius="8"
                                MinHeight="28"
                                Padding="8,3">
                            <TextBlock Text="طباعة A4" FontSize="11"/>
                        </Button>

                        <Button Click="SaveInvoiceButton_Click"
                                IsEnabled="{Binding CanSaveInvoice}"
                                Background="#EAFBF3"
                                Foreground="#166534"
                                BorderBrush="#A7E0BC"
                                BorderThickness="1"
                                CornerRadius="8"
                                MinHeight="28"
                                Padding="8,3">
                            <TextBlock Text="حفظ الفاتورة" FontSize="11"/>
                        </Button>

                        <Button Click="SavePdfButton_Click"
                                Background="#FFF4E8"
                                Foreground="#9A4A00"
                                BorderBrush="#F5C58A"
                                BorderThickness="1"
                                CornerRadius="8"
                                MinHeight="28"
                                Padding="8,3">
                            <TextBlock Text="حفظ PDF" FontSize="11"/>
                        </Button>
                    </StackPanel>

                    <StackPanel Grid.Column="0" HorizontalAlignment="Right" Spacing="1">
                        <TextBlock Text="معاينة الطباعة بقياس A4"
                                   FontSize="16"
                                   FontWeight="Bold"
                                   Foreground="#123B66"
                                   TextAlignment="Right"/>
                        <TextBlock Text="هذه هي المعاينة المباشرة المعتمدة للفواتير مع أوامر الحفظ والطباعة فقط."
                                   FontSize="10"
                                   Foreground="#52708E"
                                   Visibility="Collapsed"
                                   TextAlignment="Right"
                                   TextWrapping="WrapWholeWords"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Grid x:Name="PdfRenderHost"
                  Grid.Row="1"
                  HorizontalAlignment="Left"
                  VerticalAlignment="Top"
                  Opacity="0"
                  IsHitTestVisible="False"
                  FlowDirection="LeftToRight"/>

            <Border Grid.Row="1"
                    Background="#FFFFFF"
                    BorderBrush="#D9E6F4"
                    BorderThickness="1"
                    CornerRadius="16"
                    Padding="4"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    FlowDirection="LeftToRight">
                <ScrollViewer HorizontalScrollBarVisibility="Auto"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollMode="Auto"
                              VerticalScrollMode="Auto"
                              ZoomMode="Disabled"
                              HorizontalAlignment="Stretch"
                              VerticalAlignment="Stretch"
                              Padding="12,4,12,4">
                    <Grid HorizontalAlignment="Center"
                          VerticalAlignment="Stretch"
                          FlowDirection="LeftToRight">
                        <Border Width="794"
                                Height="1123"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Top"
                                Margin="0"
                                FlowDirection="LeftToRight"
                                Background="White"
                                BorderBrush="#C8DCEF"
                                BorderThickness="1"
                                CornerRadius="10">
                            <Grid HorizontalAlignment="Stretch"
                                  VerticalAlignment="Stretch"
                                  FlowDirection="LeftToRight">
                                <WebView2 x:Name="A4PrintWebView"
                                          DefaultBackgroundColor="White"
                                          FlowDirection="LeftToRight"
                                          HorizontalAlignment="Stretch"
                                          VerticalAlignment="Stretch"/>
                            </Grid>
                        </Border>
                    </Grid>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</Page>
